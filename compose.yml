services:
  app:
    image: node:20-alpine
    container_name: express_pos_app_${APP_ENV}
    working_dir: /app
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - ${ENV_FILE}
    command: sh -c "npm install --include=dev && npm run db:up && node index.js"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
      - public_uploads:/home/smarteat/public
    ports:
      - "5005:5005"

  db:
    image: mysql:8.0.33
    container_name: express_pos_db_${APP_ENV}
    restart: unless-stopped
    command: --default-authentication-plugin=mysql_native_password
    env_file:
      - ${ENV_FILE}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./db/mysql/init:/docker-entrypoint-initdb.d:ro
      - ./db/mysql/conf.d:/etc/mysql/conf.d:ro
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mysqladmin ping -h 127.0.0.1 -uroot -p$$MYSQL_ROOT_PASSWORD || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: express_pos_phpmyadmin_${APP_ENV}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - ${ENV_FILE}
    ports:
      - "8089:80"

volumes:
  mysql_data:
  public_uploads:
  node_modules:
